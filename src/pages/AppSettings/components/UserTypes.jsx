import React from 'react'
import Template from '@/pages/Template'
import userTypes from '@/attributes/userTypes'

const UserTypes = () => {
    return (
        <Template {...userTypes} />
    )
}

export default UserTypes
